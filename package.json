{
  "name": "next-algolia-server",
  "version": "0.2.0",
  "license": "MIT",
  "repository": "git@github.com:enBonnet/next-algolia-server.git",
  "author": "Ender Bonnet <@enBonnet>",
  "files": [
    "./lib"
  ],
  "main": "./lib/cjs/index.js",
  "module": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "yarn clean:lib && yarn build:esm && yarn build:cjs",
    "build:esm": "tsc",
    "build:cjs": "tsc --module commonjs --outDir lib/cjs",
    "clean:lib": "rm -rf lib/*"
  },
  "devDependencies": {
    "typescript": "^4.3.2"
  },
  "peerDependencies": {
    "algoliasearch": "^4.10.3"
  }
}
